spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: https://github.com/prathmeshfanse/lovable-config-server.git
          username: prathmeshfanse
          password: ${GIT_SECRET_KEY}
          default-label: main

server:
  port: 8888